# Lab M2 Playwright — aluno: jacksonsmith (teste)

5 testes E2E Playwright com:
- Locators recomendados (getByRole, getByText, getByTestId)
- storageState configurado
- Visual regression (toHaveScreenshot)
- CI workflow

## Como rodar
```bash
npm install
npx playwright install --with-deps
npx playwright test
```
