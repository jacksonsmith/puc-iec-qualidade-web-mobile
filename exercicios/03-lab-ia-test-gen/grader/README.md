# Grader — Lab IA Test Generation (20 pts)

Validator estrutural (parse-only) da rubrica do `../enunciado.md`. **Nunca executa
código da entrega** (executaria chamadas de API). A nota comentada no PR é o piso;
evidência de healing e qualidade do relatório são manuais (Canvas).

```bash
npm install
npx ts-node validator.ts --entrega ../
```

Smoke local: scaffold → baixo · gabarito+story extra+spec → alto.
